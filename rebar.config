{cover_enabled, true}.

{lib_dirs, ["deps", "apps"]}.

{erl_opts, [debug_info, warnings_as_errors, {parse_transform, lager_transform}]}.

{deps, [{lager, ".*", {git, "git://github.com/basho/lager", {tag, "2.0.0"}}},
        {eper, ".*", {git, "git://github.com/basho/eper", {tag, "1.0.0"}}},
        {druuid, ".*", {git, "git://github.com/kellymclaughlin/druuid", {tag, "0.2"}}}
]}.
